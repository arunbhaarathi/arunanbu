
## Plan: Switch Hero Text Font to Poppins

### Changes

1. **`src/index.css`** — Add Poppins to the Google Fonts import (weights 300-900)
2. **`tailwind.config.ts`** — Add `poppins` to `fontFamily` in the theme
3. **`src/components/Hero.tsx`** — Apply `font-poppins` to the hero text elements ("Hi, I am", "Arun Anbu", and the typewriter text)
