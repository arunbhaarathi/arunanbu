

## Plan: Enlarge Profile Image

**File: `src/components/Hero.tsx`**

Increase the blob container dimensions by ~50%:
- Current: `w-56 h-64` (mobile) / `sm:w-64 sm:h-[19rem]`
- New: `w-72 h-80` (mobile) / `sm:w-80 sm:h-[24rem]`

The image stays centered between text and social icons. The `object-cover` and blob border-radius remain unchanged.

